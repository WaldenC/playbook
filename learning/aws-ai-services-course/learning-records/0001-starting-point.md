# Starting point: software engineer, 5-layer AI stack already known

The user is a software engineer and has already authored the ai-infra-layers overview page (five layers: Application / Platform / Serving runtime / Cluster & systems / Silicon & hardware), so the stack-level map — including where Bedrock (Platform), SageMaker (Platform→Cluster), and UltraServers (Silicon & hardware) sit — is prior knowledge, not something to re-teach from zero.

**Implications:** lessons start one level deeper than the stack diagram; skip basic CS explanations entirely; the mission is general understanding (what each service sells, what SDEs on those teams solve daily), with interview framing explicitly out of scope as of 2026-08-16.
